

const Button = ({name}) => {
    return (
        <div>
            <button className="btn btn-outline border-0 border-b-4 btn-warning">{name}</button>
        </div>
    );
};

export default Button;